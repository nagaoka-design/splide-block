{
  "name": "splide-block-plugin",
  "version": "1.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "wp-scripts build",
    "start": "wp-scripts start",
    "package": "rm -rf dist && mkdir -p dist/splide-block && cp -R splide-block.php assets build README.md LICENSE.txt readme.txt dist/splide-block && cd dist && zip -r ../splide-block-$(node -p \"require('../package.json').version\").zip splide-block && cd .. && rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "GPL-2.0-or-later",
  "devDependencies": {
    "@wordpress/scripts": "^31.1.0"
  }
}
